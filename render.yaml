services:
  - type: web
    name: day4-flask
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python seed.py && gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
